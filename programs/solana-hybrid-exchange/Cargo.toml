[package]
name = "solana-hybrid-exchange"
version = "0.1.0"
description = "Hybrid off-chain matching, on-chain settlement exchange"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "solana_hybrid_exchange"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]
anchor-debug = []
custom-heap = []
custom-panic = []

[dependencies]
anchor-lang = { version = "1.0.2", features = ["init-if-needed"] }
anchor-spl = { version = "1.0.2", features = ["token_2022", "token_2022_extensions"] }
solana-instructions-sysvar = "3.0.0"
solana-sha256-hasher = "3.1.0"

[dev-dependencies]
litesvm = { version = "0.11", features = ["precompiles"] }
solana-message = "3.0.1"
solana-transaction = "3.0.2"
solana-signer = "3.0.0"
solana-keypair = "3.0.1"
spl-token-2022 = { version = "11", default-features = false, features = ["no-entrypoint"] }

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(target_os, values("solana"))'] }
